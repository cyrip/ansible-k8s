---
# tasks file for common
- name: Remove cloud init
  become: true
  apt:
    name: cloud-init
    state: absent

- name: Remove cloud init files and directories
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - /etc/cloud
    - /var/lib/cloud
